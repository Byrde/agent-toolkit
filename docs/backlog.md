# Project Backlog


| Epic                               | Task Description                              | Acceptance Criteria                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Status          | Prototype | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ---------------------------------- | --------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Workspace Config**               | Define config schema and loader               | A JSON schema exists for `.agent-toolkit.json`. `loadConfig(cwd)` reads the config from the project directory and returns a typed `WorkspaceConfig` object. Returns sensible defaults when no config file exists. No cascade — single file per project.                                                                                                                                                                                                                                                               | `Complete`      |           | Evolve existing manifest loader. Drop global/cascade logic. Schema must support: servers list with per-server toolset overrides, workflow hints.                                                                                                                                                                                                                                                                                                                                                              |
| **Workspace Config**               | Define workflow hints schema                  | The config schema supports a `workflows` section where developers declare how tools relate (e.g., `{"terraform": {"backend": "gcs", "ci": "github-actions"}}`). Hints are typed and validated. Loader exposes them on the `WorkspaceConfig` object.                                                                                                                                                                                                                                                                   | `Complete`      |           | This is the key input for intelligent instruction generation. Start simple — we can evolve the schema as we learn what hints matter most.                                                                                                                                                                                                                                                                                                                                                                     |
| **Auth Doctor**                    | Implement GitHub auth check                   | `checkGitHubAuth()` shells out to `gh auth status` (or validates a PAT via GitHub API) and returns `AuthResult` with pass/fail and remediation steps.                                                                                                                                                                                                                                                                                                                                                                 | `Complete`      |           | Depends on config telling us GitHub is required.                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Auth Doctor**                    | Implement GCP auth check                      | `checkGcpAuth()` validates `gcloud auth` status or application default credentials. Returns `AuthResult`.                                                                                                                                                                                                                                                                                                                                                                                                             | `Complete`      |           |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Auth Doctor**                    | Implement Terraform Cloud auth check          | `checkTerraformAuth()` validates TFE token (env var or `~/.terraform.d/credentials.tfrc.json`) against the configured TFE address. Returns `AuthResult`.                                                                                                                                                                                                                                                                                                                                                              | `Complete`      |           |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Auth Doctor**                    | Implement Atlassian auth check                | `checkAtlassianAuth()` verifies connectivity to Atlassian MCP endpoint or validates an API token. Returns `AuthResult` with OAuth flow guidance on failure.                                                                                                                                                                                                                                                                                                                                                           | `Complete`      |           | Atlassian uses OAuth 2.1 browser flow — auth check may be limited to endpoint reachability.                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Auth Doctor**                    | Wire `doctor` CLI command                     | `agent-toolkit doctor` reads the config, runs all applicable auth checks, and prints a summary table (provider / status / remediation). Non-zero exit on any failure.                                                                                                                                                                                                                                                                                                                                                 | `Complete`      |           |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Onboarding CLI**                 | Consolidate CLI into `init` + `doctor`        | `agent-toolkit init` is the single interactive entrypoint for both initial repo setup and individual developer onboarding. Flow: (1) configure — if `.agent-toolkit.json` exists, offer to reconfigure; if not, run interactive setup. (2) doctor — validate auth, halt with remediation on failure. (2b) identity pinning — for each authenticated provider, present account selection (rich picker for GitHub/GCP, confirm for others) and write `.agent-toolkit.local.json`. (3) install — prompt for AI client, write client config + instructions + gitignore. `agent-toolkit doctor` remains standalone for verification/repair. `setup` and `install` are removed as standalone commands. | `Complete`      |           | Replaces the previous `setup`, `install`, and `init` tasks. Key change: `init` always offers reconfiguration instead of silently skipping. `init` now generates and writes agent instructions from server usage notes. Identity pinning added: GitHub and GCP use `listGitHubAccounts`/`listGcpAccounts` for multi-account picker; Terraform and Atlassian use confirm with detected identity. `editor()` bug resolved by switching to `input()`. Bug fix during QA: `main.ts` was calling `program.parse()` without `await`, causing the process to exit before async operations (instruction writing) completed via the compiled `npx` path. Fixed to `await program.parseAsync()`. |
| **Multi-Client Config Generation** | Implement Cursor config generator             | Given a resolved config and auth report, generates `.cursor/mcp.json` with the correct `mcpServers` entries for each declared server. Merges with existing config (preserves user-added servers).                                                                                                                                                                                                                                                                                                                     | `Complete`      |           | Cursor supports `url` for remote servers and `command`/`args`/`env` for local servers.                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Multi-Client Config Generation** | Implement Claude Code config generator        | Generates `.mcp.json` (project-level) for Claude Code with the correct server entries.                                                                                                                                                                                                                                                                                                                                                                                                                                | `Complete`      |           |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Multi-Client Config Generation** | Implement VS Code config generator            | Generates `.vscode/mcp.json` with server entries in VS Code's format (includes `inputs` array for token prompts).                                                                                                                                                                                                                                                                                                                                                                                                     | `Complete`      |           | VS Code format wraps servers under a `servers` key and supports `inputs` for interactive token entry.                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Multi-Client Config Generation** | Implement config merge strategy               | Config generators read existing client config, merge toolkit-managed servers (identified by naming convention or marker), and preserve user-added servers. Toolkit-managed servers are updated in place on re-run.                                                                                                                                                                                                                                                                                                    | `Complete`      |           | Critical for idempotency.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **Multi-Client Config Generation** | Wire `install` CLI command                    | `agent-toolkit install --client <target>` reads config, runs doctor, generates config + instructions, and writes files. Reports what was written.                                                                                                                                                                                                                                                                                                                                                                     | `Complete`      |           | Superseded by consolidated `init` command. Install logic moves into `init` step 3. Standalone `install` command will be removed.                                                                                                                                                                                                                                                                                                                                                                              |
| **End-to-End Verification**        | Add verification step to `init` and `doctor`  | After `init` completes install, a verification step confirms that client config files exist and contain the expected MCP server entries. `doctor --client <target>` verifies the specified client config as part of its diagnostics, with remediation on failure. No standalone `verify` command — consistent with the `init` + `doctor` consolidation. AI-client diagnostic (next four tasks) runs as part of the standard `init` and `doctor` flow.                                                                 | `Complete`      |           | Keeps the two-command surface (`init` + `doctor`). Both commands require explicit `--client` or interactive prompt — no auto-detection. Missing servers get remediation pointing to `agent-toolkit init`. Client diagnostic runs inline as the final step — no separate flag needed.                                                                                                                                                                                                                         |
| **AI Client Diagnostic**           | Define client CLI registry and availability check | A `ClientCliSpec` registry maps each `ClientTarget` to its CLI binary name, headless flags, and JSON output flags. `checkCliAvailable(client)` shells out to locate the binary and returns availability status with remediation if missing (e.g., "Install Claude Code: `npm install -g @anthropic-ai/claude-code`"). Covers: `claude` (Claude Code), `agent` (Cursor CLI), `copilot` (VS Code Copilot), `codex` (Codex), `gemini` (Gemini CLI).                                                                 | `Complete`      |           | Foundational for the client diagnostic feature. Registry is a simple map — no external dependencies. Availability check uses `which` / `command -v`. Remediation messages should include the install command for each client CLI. Cursor uses the `agent` binary from Cursor CLI (`curl https://cursor.com/install`), not the `cursor` IDE launcher.                                                                                                                                                                   |
| **AI Client Diagnostic**           | Implement diagnostic runner                   | `runDiagnostic(client, config, cwd)` invokes the client CLI in headless mode with a structured diagnostic prompt. The prompt instructs the AI to: (1) list all MCP tools it can see, (2) paraphrase how each tool should be used per the agent instructions, (3) execute the most basic read-only command for each configured server. Returns raw CLI output. Handles timeout (60s), non-zero exit, and stderr capture.                                                                                              | `Complete`      |           | Prompt must request structured JSON output for reliable parsing. Use `child_process.execFile` with timeout. The diagnostic prompt is derived from the workspace config — tool names and expected usage come from the config and generated instructions. Basic commands: GitHub → list repos, GCP → list projects, Terraform → show workspaces, Atlassian → list projects, Figma → list files.                                                                                                                   |
| **AI Client Diagnostic**           | Implement validation runner                   | `runValidation(client, diagnosticOutput, config, cwd)` invokes a second headless CLI instance with a validation prompt. The prompt provides: (1) the expected tool list from the workspace config, (2) expected usage summaries from the generated instructions, (3) the diagnostic output from the first instance. The validator judges each tool on three criteria: listed, paraphrase accurate, basic command passed. Returns a structured `ClientDiagnosticResult` with per-tool verdicts and an overall pass/fail.    | `Complete`      |           | Judge/evaluator pattern — the second instance independently validates the first. Expected answers are deterministic (derived from config), so the validator has a clear rubric. JSON parse failure falls back to text analysis with partial results. This judge/evaluator pattern is reusable for the LLM-driven instruction generator task.                                                                                                                                                                                    |
| **AI Client Diagnostic**           | Wire client diagnostic into `init` and `doctor` | `init` runs the client diagnostic as step 5 after config verification. `doctor --client <target>` runs the diagnostic as its final step after auth checks and config verification. Both require explicit client selection (via `--client` flag or interactive prompt) — no auto-detection. Both print a diagnostic summary table (tool / listed / usage accurate / command OK / status). Diagnostic failure prints warnings and remediation but does not block `init` completion (config is already installed). `doctor` exits non-zero if diagnostic fails. CLI availability failure short-circuits with install remediation. | `Complete`      |           | Client diagnostic is part of the standard flow — no opt-in flag. `init` treats diagnostic failure as a warning (exit 0) since the config is already written. `doctor` treats it as a failure (exit 1). Table format mirrors the existing doctor auth table for consistency. `doctor` no longer uses `detectInstalledClients` — always prompts for or requires `--client`.                                                                                                                                     |
| **Intelligent Agent Instructions** | Implement LLM-driven instruction generator    | Given a `WorkspaceConfig` (servers, workflow hints), invokes the user's AI client to generate contextual agent instructions: per-server best practices, cross-server orchestration patterns, and context-gathering guidance. Output is client-agnostic Markdown. Reuses the client CLI registry and headless invocation pattern established by the client diagnostic feature.                                                                                                                                               | `Ready to Test` |           | Core differentiator. The LLM generates instructions based on the toolkit config — no static templates. Workflow hints provide the context that makes instructions intelligent. Implemented in `packages/core/src/instruction-generator.ts`. `buildInstructionPrompt` builds a detailed prompt from config (servers, usage notes, workflow hints). `generateLlmInstructions` invokes the AI client CLI in headless mode with streaming support. `cli.ts` uses `resolveInstructions` which tries LLM generation first, falling back to static `buildStaticInstructions` on failure. `spawnCli` timeout made configurable (180s for generation vs 120s for diagnostics).                                                                                                                                                                                  |
| **Intelligent Agent Instructions** | Implement Cursor rules writer                 | Writes LLM-generated instructions into a dedicated `.cursor/rules/agent-toolkit.mdc` file with correct frontmatter. Does not touch other user-authored rule files. Idempotent on re-run.                                                                                                                                                                                                                                                                                                                              | `Complete`      |           | Cursor supports multiple rule files — use a dedicated file.                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Intelligent Agent Instructions** | Implement Claude Code instructions writer     | Writes LLM-generated instructions into a fenced section in `CLAUDE.md`. Preserves existing user content outside the `<!-- BEGIN agent-toolkit -->` / `<!-- END agent-toolkit -->` markers.                                                                                                                                                                                                                                                                                                                            | `Complete`      |           | Single instruction file — use fenced section strategy.                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Intelligent Agent Instructions** | Implement VS Code Copilot instructions writer | Writes LLM-generated instructions into a fenced section in `.github/copilot-instructions.md`. Preserves existing user content outside markers.                                                                                                                                                                                                                                                                                                                                                                        | `Complete`      |           | Single instruction file — use fenced section strategy.                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Intelligent Agent Instructions** | Implement Codex instructions writer           | Writes LLM-generated instructions into a fenced section in `AGENTS.md`. Preserves existing user content outside markers.                                                                                                                                                                                                                                                                                                                                                                                              | `Complete`      |           | Single instruction file — use fenced section strategy.                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Intelligent Agent Instructions** | Implement Gemini instructions writer          | Writes LLM-generated instructions into a fenced section in `GEMINI.md`. Preserves existing user content outside markers.                                                                                                                                                                                                                                                                                                                                                                                              | `Complete`      |           | Single instruction file — use fenced section strategy.                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Auth Doctor**                    | Per-project account selection                 | A gitignored local config (e.g., `.agent-toolkit.local.json`) lets developers pin which account/identity to use per provider in this project. Auth checks validate against the pinned identity when present. `init` captures identities interactively: GitHub/GCP present a multi-account picker when multiple accounts are authenticated; Terraform/Atlassian confirm the detected identity. `writeLocalConfig` merges with existing local config.                                                                     | `Complete`      |           | Cross-cutting concern across all providers. Needed when developers have multiple accounts (e.g., personal + org GitHub, multiple GCP projects). Identity capture wired into `init` step 2b. `listGitHubAccounts` parses `gh auth status`, `listGcpAccounts` parses `gcloud auth list --format=json`. `extractPinnableIdentity` converts `AuthStatus` to the pinnable shape. `writeLocalConfig` merges with existing file.                                                                                      |
| **Project-Level Scoping**          | Verify zero-bleed isolation                   | Running `agent-toolkit install` in `/projects/foo` writes config only to `/projects/foo`. An AI client session started in `/home/user` (no config) has no toolkit-generated MCP servers or instructions. Verified by integration test.                                                                                                                                                                                                                                                                                | `Complete` |           | This is a constraint, not a feature to build — but needs explicit verification. Integration test at `packages/core/src/isolation.test.ts` exercises all write functions (3 config generators, 5 instruction writers, gitignore, local config) against a target directory and asserts zero artifacts leak to a sibling directory.                                                                                                                                                                                                                                                                                                                                                                                                                               |


## Column Guide

- **Status**: Must follow `TODO` → `In Progress` → `Ready to Test` → `Complete`
- **Prototype**: Maintained by prototyping workflow. Leave empty for new tasks. When prototype exists: `prototypes/[slug].[ext]` + brief findings.
- **Notes**: Include testability considerations for non-trivial verification:
  - If feature involves long-running processes, note start/verify/stop pattern needed
  - If feature requires user interaction for testing, note the handoff scenario
  - If verification is straightforward, no note needed

